"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const graphql = require("@prismicio/types/dist/graphql");
const asLink = (linkField, linkResolver) => {
  if (!linkField) {
    return null;
  }
  if ("url" in linkField) {
    return linkField.url;
  } else if (linkField._linkType === graphql.LinkType.Document) {
    return linkResolver ? linkResolver(linkField) : null;
  } else {
    return null;
  }
};
exports.asLink = asLink;
//# sourceMappingURL=asLink.cjs.map
